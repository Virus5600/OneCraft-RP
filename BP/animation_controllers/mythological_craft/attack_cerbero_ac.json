{
  "format_version": "1.10.0",
  "animation_controllers": {
    "controller.animation.mythological_craft.cerbero_attack": {
      "initial_state": "default",
      "states": {
        "default": {
          "transitions": [ 
            { 
              "attack": "query.is_delayed_attacking && !query.is_stunned"
            }
          ]
        },
        "attack": {
          "transitions": [ 
            { 
              "default": "!query.is_delayed_attacking && !query.is_stunned" 
            } 
          ],
          "on_exit": [ 
            "@s ocrp:entity_attack" 
          ]
        }
      }
    }
  }
}
