{
	"format_version": "1.10.0",
	"minecraft:client_entity": {
		"description": {
			"identifier": "ocrp:grappling_hook_projectile",
			"materials": {
				"default": "arrow"
			},
			"textures": {
				"default": "textures/entity/roll_and_stamina/grappling_hook"
			},
			"geometry": {
				"default": "geometry.grappling_hook"
			},
			"animations": {
				"rot": "animation.grappling_hook.rotation"
			},
			"scripts": {
				"animate": [
					"rot"
				],
				"initialize": [
					"v.distance = query.property('hook:distance');",
					"v.x_rot = query.property('hook:x_rot');",
					"v.y_rot = query.property('hook:y_rot');"
				],
				"pre_animation": [
					"(math.abs(v.distance - query.property('hook:distance')) > 10) ? { v.distance = query.property('hook:distance'); } : { v.distance = math.lerp(v.distance, query.property('hook:distance'), 0.5); };",
					"(math.abs(v.x_rot - query.property('hook:x_rot')) > 25) ? { v.x_rot = query.property('hook:x_rot'); } : { v.x_rot = math.lerp(v.x_rot, query.property('hook:x_rot'), 0.5); };",
					"(math.abs(v.y_rot - query.property('hook:y_rot')) > 25) ? { v.y_rot = query.property('hook:y_rot'); } : { v.y_rot = math.lerp(v.y_rot, query.property('hook:y_rot'), 0.5); };"
				]
			},
			"render_controllers": [
				"controller.render.default"
			]
		}
	}
}