{
	"format_version": "1.10.0",
	"animation_controllers": {
		"controller.animation.mythological_craft.pandora_box": {
			"initial_state": "default",
			"states": {
				"default": {
					"animations": [
						{
						  "open_box": "(query.ground_speed == 0.0 && (query.is_on_ground || query.is_in_water))"
						}
					]
				}
			}
		}
	}
}