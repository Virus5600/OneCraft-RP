{
	"format_version": "1.10.0",
	"animation_controllers": {
		"controller.animation.mythological_craft.mc_cerbero": {
			"initial_state": "default",
			"states": {
				"default": {
					"animations": [
						{
						  	"idle": "(query.ground_speed == 0.0 && query.is_on_ground && !query.is_sheared && !query.is_delayed_attacking && !query.is_stunned && !query.is_sitting) || (query.mark_variant == 2 && query.is_delayed_attacking && !query.is_sitting)"
						},
						{
						  	"attack2": "query.mark_variant == 2 && query.is_delayed_attacking"
						}
					],
					"transitions": [
					   	{
						 	"move": "query.ground_speed > 0.0 && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned && !query.is_sitting"
					   	},
						{
							"sleep": "query.is_stunned && !query.is_delayed_attacking && !query.is_sheared"
						},
						{
							"up": "query.is_sheared && !query.is_delayed_attacking && !query.is_stunned && !query.is_sitting"
						},
						{
							"sit": "query.is_sitting && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned"
						},
					   	{
						 	"attack1": "query.mark_variant == 1 && query.is_delayed_attacking"
					  	},
					   	{
						 	"attack3": "query.mark_variant == 3 && query.is_delayed_attacking"
					  	},
					   	{
						 	"attack4": "query.mark_variant == 4 && query.is_delayed_attacking"
					  	},
					   	{
						 	"attack5": "query.mark_variant == 5 && query.is_delayed_attacking"
					  	}
					]
				},
				"move": {
					"animations": [
						"move"
					],
					"transitions": [
					 	{
					   		"default": "(query.ground_speed == 0.0 && query.is_on_ground && !query.is_sheared && !query.is_delayed_attacking && !query.is_stunned && !query.is_sitting) || (query.mark_variant == 2 && query.is_delayed_attacking && !query.is_sitting)"
					 	},
						{
							"sleep": "query.is_stunned && !query.is_delayed_attacking && !query.is_sheared"
						},
						{
							"up": "query.is_sheared && !query.is_delayed_attacking && !query.is_stunned && !query.is_sitting"
						},
						{
							"sit": "query.is_sitting && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned"
						},
					 	{
					  	 	"attack1": "query.mark_variant == 1 && query.is_delayed_attacking"
					 	},
						{
							"attack3": "query.mark_variant == 3 && query.is_delayed_attacking"
						},
						{
							"attack4": "query.mark_variant == 4 && query.is_delayed_attacking"
						},
						{
							"attack5": "query.mark_variant == 5 && query.is_delayed_attacking"
						}
					]
				},
				"attack1": {
					"animations": [
					  "attack1"
					],
					"transitions": [
					 	{
					   		"default": "(query.ground_speed == 0.0 && query.is_on_ground && !query.is_sheared && !query.is_delayed_attacking && !query.is_stunned && !query.is_sitting) || (query.mark_variant == 2 && query.is_delayed_attacking && !query.is_sitting)"
					 	},
					 	{
					   		"move": "query.ground_speed > 0.0 && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned && !query.is_sitting"
					 	},
						{
							"sleep": "query.is_stunned && !query.is_delayed_attacking && !query.is_sheared"
						},
						{
							"up": "query.is_sheared && !query.is_delayed_attacking && !query.is_stunned && !query.is_sitting"
						},
						{
							"sit": "query.is_sitting && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned"
						},
						{
							"attack3": "query.mark_variant == 3 && query.is_delayed_attacking"
						},
						{
							"attack4": "query.mark_variant == 4 && query.is_delayed_attacking"
						},
						{
							"attack5": "query.mark_variant == 5 && query.is_delayed_attacking"
						}
					]
				},
				"attack3": {
					"animations": [
					  "attack3"
					],
					"transitions": [
					 	{
					   		"default": "(query.ground_speed == 0.0 && query.is_on_ground && !query.is_sheared && !query.is_delayed_attacking && !query.is_stunned && !query.is_sitting) || (query.mark_variant == 2 && query.is_delayed_attacking && !query.is_sitting)"
					 	},
					 	{
					   		"move": "query.ground_speed > 0.0 && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned && !query.is_sitting"
					 	},
						{
							"sleep": "query.is_stunned && !query.is_delayed_attacking && !query.is_sheared"
						},
						{
							"up": "query.is_sheared && !query.is_delayed_attacking && !query.is_stunned && !query.is_sitting"
						},
						{
							"sit": "query.is_sitting && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned"
						},
						{
							"attack1": "query.mark_variant == 1 && query.is_delayed_attacking"
						},
						{
							"attack4": "query.mark_variant == 4 && query.is_delayed_attacking"
						},
						{
							"attack5": "query.mark_variant == 5 && query.is_delayed_attacking"
						}
					]
				},
				"attack4": {
					"animations": [
					  "attack4"
					],
					"transitions": [
					 	{
					   		"default": "(query.ground_speed == 0.0 && query.is_on_ground && !query.is_sheared && !query.is_delayed_attacking && !query.is_stunned && !query.is_sitting) || (query.mark_variant == 2 && query.is_delayed_attacking && !query.is_sitting)"
					 	},
					 	{
					   		"move": "query.ground_speed > 0.0 && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned && !query.is_sitting"
					 	},
						{
							"sleep": "query.is_stunned && !query.is_delayed_attacking && !query.is_sheared"
						},
						{
							"up": "query.is_sheared && !query.is_delayed_attacking && !query.is_stunned && !query.is_sitting"
						},
						{
							"sit": "query.is_sitting && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned"
						},
						{
							"attack3": "query.mark_variant == 3 && query.is_delayed_attacking"
						},
						{
							"attack1": "query.mark_variant == 1 && query.is_delayed_attacking"
						},
						{
							"attack5": "query.mark_variant == 5 && query.is_delayed_attacking"
						}
					]
				},
				"attack5": {
					"animations": [
					  "attack5"
					],
					"transitions": [
					 	{
					   		"default": "(query.ground_speed == 0.0 && query.is_on_ground && !query.is_sheared && !query.is_delayed_attacking && !query.is_stunned && !query.is_sitting) || (query.mark_variant == 2 && query.is_delayed_attacking && !query.is_sitting)"
					 	},
					 	{
					   		"move": "query.ground_speed > 0.0 && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned && !query.is_sitting"
					 	},
						{
							"sleep": "query.is_stunned && !query.is_delayed_attacking && !query.is_sheared"
						},
						{
							"up": "query.is_sheared && !query.is_delayed_attacking && !query.is_stunned && !query.is_sitting"
						},
						{
							"sit": "query.is_sitting && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned"
						},
						{
							"attack3": "query.mark_variant == 3 && query.is_delayed_attacking"
						},
						{
							"attack4": "query.mark_variant == 4 && query.is_delayed_attacking"
						},
						{
							"attack1": "query.mark_variant == 1 && query.is_delayed_attacking"
						}
					]
				},
				"sleep": {
					"animations": [
					  "sleep"
					],
					"transitions": [
					 	{
					   		"default": "(query.ground_speed == 0.0 && query.is_on_ground && !query.is_sheared && !query.is_delayed_attacking && !query.is_stunned && !query.is_sitting) || (query.mark_variant == 2 && query.is_delayed_attacking && !query.is_sitting)"
					 	},
					 	{
					   		"move": "query.ground_speed > 0.0 && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned && !query.is_sitting"
					 	},
						{
							"up": "query.is_sheared && !query.is_delayed_attacking && !query.is_stunned && !query.is_sitting"
						},
						{
							"sit": "query.is_sitting && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned"
						},
						{
							"attack1": "query.mark_variant == 1 && query.is_delayed_attacking"
						},
						{
							"attack3": "query.mark_variant == 3 && query.is_delayed_attacking"
						},
						{
							"attack4": "query.mark_variant == 4 && query.is_delayed_attacking"
						},
						{
							"attack5": "query.mark_variant == 5 && query.is_delayed_attacking"
						}
					]
				},
				"up": {
					"animations": [
					  "up"
					],
					"transitions": [
						{
							"default": "(query.ground_speed == 0.0 && query.is_on_ground && !query.is_sheared && !query.is_delayed_attacking && !query.is_stunned && !query.is_sitting) || (query.mark_variant == 2 && query.is_delayed_attacking && !query.is_sitting)"
						},
						{
							"move": "query.ground_speed > 0.0 && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned && !query.is_sitting"
						},
						{
							"sleep": "query.is_stunned && !query.is_delayed_attacking && !query.is_sheared"
						},
						{
							"sit": "query.is_sitting && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned"
						},
						{
							"attack1": "query.mark_variant == 1 && query.is_delayed_attacking"
						},
						{
							"attack3": "query.mark_variant == 3 && query.is_delayed_attacking"
						},
						{
							"attack4": "query.mark_variant == 4 && query.is_delayed_attacking"
						},
						{
							"attack5": "query.mark_variant == 5 && query.is_delayed_attacking"
						}
					]
				},
				"sit": {
					"animations": [
					  "sleep"
					],
					"transitions": [
						{
							"default": "(query.ground_speed == 0.0 && query.is_on_ground && !query.is_sheared && !query.is_delayed_attacking && !query.is_stunned && !query.is_sitting) || (query.mark_variant == 2 && query.is_delayed_attacking && !query.is_sitting)"
						},
						{
							"move": "query.ground_speed > 0.0 && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned && !query.is_sitting"
						},
						{
							"sleep": "query.is_stunned && !query.is_delayed_attacking && !query.is_sheared"
						},
						{
							"up": "query.is_sheared && !query.is_delayed_attacking && !query.is_stunned && !query.is_sitting"
						},
						{
							"attack1": "query.mark_variant == 1 && query.is_delayed_attacking"
						},
						{
							"attack3": "query.mark_variant == 3 && query.is_delayed_attacking"
						},
						{
							"attack4": "query.mark_variant == 4 && query.is_delayed_attacking"
						},
						{
							"attack5": "query.mark_variant == 5 && query.is_delayed_attacking"
						}
					]
				}
			}
		}
	}
}