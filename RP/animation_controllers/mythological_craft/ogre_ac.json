{
	"format_version": "1.10.0",
	"animation_controllers": {
		"controller.animation.mythological_craft.mc_ogre": {
			"initial_state": "default",
			"states": {
				"default": {
					"animations": [
						{
						  	"idle": "(query.ground_speed == 0.0 && (query.is_on_ground || query.is_in_water) && !query.is_delayed_attacking && !query.is_stunned && !query.is_sheared) || (query.mark_variant == 2 && query.is_delayed_attacking && !query.is_stunned && !query.is_sheared)"
						},
						{
						  	"attack2": "query.mark_variant == 2 && query.is_delayed_attacking"
						}
					],
					"transitions": [
					   	{
						 	"move": "query.ground_speed > 0.0 && !query.is_delayed_attacking && !query.is_stunned && query.is_on_ground && !query.is_sheared"
					   	},
					   	{
						 	"attack1": "query.mark_variant == 1 && query.is_delayed_attacking"
					   	},
						{
							"roaring": "query.is_stunned && !query.is_delayed_attacking && !query.is_sheared"
					  	},
						{
							"attack3": "query.is_sheared && !query.is_delayed_attacking && !query.is_stunned"
					  	}
					]
				},
				"move": {
					"animations": [
						"move"
					],
					"transitions": [
					 	{
					   		"default": "(query.ground_speed == 0.0 && (query.is_on_ground || query.is_in_water) && !query.is_delayed_attacking && !query.is_stunned && !query.is_sheared) || (query.mark_variant == 2 && query.is_delayed_attacking && !query.is_stunned && !query.is_sheared)"
					 	},
						{
							"attack1": "query.mark_variant == 1 && query.is_delayed_attacking"
						},
					 	{
							"roaring": "query.is_stunned && !query.is_delayed_attacking && !query.is_sheared"
					  	},
						{
							"attack3": "query.is_sheared && !query.is_delayed_attacking && !query.is_stunned"
					  	}
					]
				},
				"roaring": {
					"animations": [
					  "roaring"
					],
					"transitions": [
					 	{
					   		"default": "(query.ground_speed == 0.0 && (query.is_on_ground || query.is_in_water) && !query.is_delayed_attacking && !query.is_stunned && !query.is_sheared) || (query.mark_variant == 2 && query.is_delayed_attacking && !query.is_stunned && !query.is_sheared)"
					 	},
						{
							"attack1": "query.mark_variant == 1 && query.is_delayed_attacking"
						},
					 	{
					   		"move": "query.ground_speed > 0.0 && !query.is_delayed_attacking && !query.is_stunned && query.is_on_ground && !query.is_sheared"
					 	},
						{
							"attack3": "query.is_sheared && !query.is_delayed_attacking && !query.is_stunned"
					  	}
					]
				},
				"attack1": {
					"animations": [
					  "attack1"
					],
					"transitions": [
					 	{
					   		"default": "(query.ground_speed == 0.0 && (query.is_on_ground || query.is_in_water) && !query.is_delayed_attacking && !query.is_stunned && !query.is_sheared) || (query.mark_variant == 2 && query.is_delayed_attacking && !query.is_stunned && !query.is_sheared)"
					 	},
					 	{
					   		"move": "query.ground_speed > 0.0 && !query.is_delayed_attacking && !query.is_stunned && query.is_on_ground && !query.is_sheared"
					 	},
						{
							"roaring": "query.is_stunned && !query.is_delayed_attacking && !query.is_sheared"
					  	},
						{
							"attack3": "query.is_sheared && !query.is_delayed_attacking && !query.is_stunned"
					  	}
					]
				},
				"attack3": {
					"animations": [
					  "attack3"
					],
					"transitions": [
					 	{
					   		"default": "(query.ground_speed == 0.0 && (query.is_on_ground || query.is_in_water) && !query.is_delayed_attacking && !query.is_stunned && !query.is_sheared) || (query.mark_variant == 2 && query.is_delayed_attacking && !query.is_stunned && !query.is_sheared)"
					 	},
					 	{
					   		"move": "query.ground_speed > 0.0 && !query.is_delayed_attacking && !query.is_stunned && query.is_on_ground && !query.is_sheared"
					 	},
						{
							"roaring": "query.is_stunned && !query.is_delayed_attacking && !query.is_sheared"
					  	},
						{
							"attack1": "query.mark_variant == 1 && query.is_delayed_attacking"
						}
					]
				}
			}
		}
	}
}