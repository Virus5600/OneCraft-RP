{
	"format_version": "1.10.0",
	"animation_controllers": {
		"controller.animation.mythological_craft.mc_cyclops": {
			"initial_state": "default",
			"states": {
				"default": {
					"animations": [
						{
						  	"idle": "(query.ground_speed == 0.0 && (query.is_on_ground || query.is_in_water) && !query.is_delayed_attacking && !query.is_stunned && !query.is_saddled) || (query.mark_variant == 2 && query.is_delayed_attacking && !query.is_stunned && !query.is_saddled)"
						},
						{
						  	"attack2": "query.mark_variant == 2 && query.is_delayed_attacking"
						}
					],
					"transitions": [
					   	{
						 	"move": "query.ground_speed > 0.0 && !query.is_delayed_attacking && !query.is_stunned && query.is_on_ground && !query.is_saddled"
					   	},
					   	{
						 	"attack1": "query.mark_variant == 1 && query.is_delayed_attacking"
					   	},
						{
							"eat": "query.is_stunned && !query.is_delayed_attacking && !query.is_saddled"
					  	},
						{
							"attack3": "query.is_saddled && !query.is_delayed_attacking && !query.is_stunned"
					  	}
					]
				},
				"move": {
					"animations": [
						"move"
					],
					"transitions": [
					 	{
					   		"default": "(query.ground_speed == 0.0 && (query.is_on_ground || query.is_in_water) && !query.is_delayed_attacking && !query.is_stunned && !query.is_saddled) || (query.mark_variant == 2 && query.is_delayed_attacking && !query.is_stunned && !query.is_saddled)"
					 	},
						{
							"attack1": "query.mark_variant == 1 && query.is_delayed_attacking"
						},
					 	{
							"eat": "query.is_stunned && !query.is_delayed_attacking && !query.is_saddled"
					  	},
						{
							"attack3": "query.is_saddled && !query.is_delayed_attacking && !query.is_stunned"
					  	}
					]
				},
				"eat": {
					"animations": [
					  "eat"
					],
					"transitions": [
					 	{
					   		"default": "(query.ground_speed == 0.0 && (query.is_on_ground || query.is_in_water) && !query.is_delayed_attacking && !query.is_stunned && !query.is_saddled) || (query.mark_variant == 2 && query.is_delayed_attacking && !query.is_stunned && !query.is_saddled)"
					 	},
						{
							"attack1": "query.mark_variant == 1 && query.is_delayed_attacking"
						},
					 	{
					   		"move": "query.ground_speed > 0.0 && !query.is_delayed_attacking && !query.is_stunned && query.is_on_ground && !query.is_saddled"
					 	},
						{
							"attack3": "query.is_saddled && !query.is_delayed_attacking && !query.is_stunned"
					  	}
					]
				},
				"attack1": {
					"animations": [
					  "attack1"
					],
					"transitions": [
					 	{
					   		"default": "(query.ground_speed == 0.0 && (query.is_on_ground || query.is_in_water) && !query.is_delayed_attacking && !query.is_stunned && !query.is_saddled) || (query.mark_variant == 2 && query.is_delayed_attacking && !query.is_stunned && !query.is_saddled)"
					 	},
					 	{
					   		"move": "query.ground_speed > 0.0 && !query.is_delayed_attacking && !query.is_stunned && query.is_on_ground && !query.is_saddled"
					 	},
						{
							"eat": "query.is_stunned && !query.is_delayed_attacking && !query.is_saddled"
					  	},
						{
							"attack3": "query.is_saddled && !query.is_delayed_attacking && !query.is_stunned"
					  	}
					]
				},
				"attack3": {
					"animations": [
					  "attack3"
					],
					"transitions": [
					 	{
					   		"default": "(query.ground_speed == 0.0 && (query.is_on_ground || query.is_in_water) && !query.is_delayed_attacking && !query.is_stunned && !query.is_saddled) || (query.mark_variant == 2 && query.is_delayed_attacking && !query.is_stunned && !query.is_saddled)"
					 	},
					 	{
					   		"move": "query.ground_speed > 0.0 && !query.is_delayed_attacking && !query.is_stunned && query.is_on_ground && !query.is_saddled"
					 	},
						{
							"eat": "query.is_stunned && !query.is_delayed_attacking && !query.is_saddled"
					  	},
						{
							"attack1": "query.mark_variant == 1 && query.is_delayed_attacking"
						}
					]
				}
			}
		}
	}
}