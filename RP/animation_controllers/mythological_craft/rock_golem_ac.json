{
	"format_version": "1.10.0",
	"animation_controllers": {
		"controller.animation.mythological_craft.rock_golem": {
			"initial_state": "default",
			"states": {
				"default": {
					"animations": [
						{
						  	"idle": "(query.ground_speed == 0.0 && query.is_on_ground && !query.is_saddled && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned && !query.is_casting) || (query.mark_variant == 2 && query.is_delayed_attacking)"
						},
						{
						  	"attack2": "query.mark_variant == 2 && query.is_delayed_attacking"
						}
					],
					"transitions": [
						{
							"move": "query.ground_speed > 0.0 && !query.is_delayed_attacking && !query.is_sheared && !query.is_saddled && !query.is_stunned && !query.is_casting"
						},
						{
							"stay": "query.is_stunned && !query.is_delayed_attacking && !query.is_saddled && !query.is_sheared && !query.is_casting"
						},
						{
							"up": "query.is_saddled && !query.is_delayed_attacking && !query.is_stunned && !query.is_sheared && !query.is_casting"
						},
						{
							"attack1": "query.mark_variant == 1 && query.is_delayed_attacking"
						},
						{
							"summon":  "query.is_casting && !query.is_delayed_attacking && !query.is_saddled && !query.is_sheared && !query.is_stunned"
						},
						{
							"throw": "query.is_sheared && !query.is_delayed_attacking"
						}
					]
				},
				"move": {
					"animations": [
						"move"
					],
					"transitions": [
					 	{
					   		"default": "(query.ground_speed == 0.0 && query.is_on_ground && !query.is_saddled && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned && !query.is_casting) || (query.mark_variant == 2 && query.is_delayed_attacking)"
					 	},
						{
							"stay": "query.is_stunned && !query.is_delayed_attacking && !query.is_saddled && !query.is_sheared && !query.is_casting"
						},
					 	{
							"up": "query.is_saddled && !query.is_delayed_attacking && !query.is_stunned && !query.is_sheared && !query.is_casting"
						},
						{
							"attack1": "query.mark_variant == 1 && query.is_delayed_attacking"
						},
						{
							"summon": "query.is_casting && !query.is_delayed_attacking && !query.is_saddled && !query.is_sheared && !query.is_stunned"
						},
						{
							"throw": "query.is_sheared && !query.is_delayed_attacking"
						}
					]
				},
				"stay": {
					"animations": [
					  "stay"
					],
					"transitions": [
					 	{
					   		"default": "(query.ground_speed == 0.0 && query.is_on_ground && !query.is_saddled && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned && !query.is_casting) || (query.mark_variant == 2 && query.is_delayed_attacking)"
					 	},
					 	{
					   		"move": "query.ground_speed > 0.0 && !query.is_delayed_attacking && !query.is_sheared && !query.is_saddled && !query.is_stunned && !query.is_casting"
					 	},
						{
							"up": "query.is_saddled && !query.is_delayed_attacking && !query.is_stunned && !query.is_sheared && !query.is_casting"
						},
						{
							"attack1": "query.mark_variant == 1 && query.is_delayed_attacking"
						},
						{
							"summon": "query.is_casting && !query.is_delayed_attacking && !query.is_saddled && !query.is_sheared && !query.is_stunned"
						},
						{
							"throw": "query.is_sheared && !query.is_delayed_attacking"
						}
					]
				},
				"up": {
					"animations": [
					  "up"
					],
					"transitions": [
					 	{
					   		"default": "(query.ground_speed == 0.0 && query.is_on_ground && !query.is_saddled && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned && !query.is_casting) || (query.mark_variant == 2 && query.is_delayed_attacking)"
					 	},
					 	{
					   		"move": "query.ground_speed > 0.0 && !query.is_delayed_attacking && !query.is_sheared && !query.is_saddled && !query.is_stunned && !query.is_casting"
					 	},
						{
							"stay": "query.is_stunned && !query.is_delayed_attacking && !query.is_saddled && !query.is_sheared && !query.is_casting"
						},
						{
							"attack1": "query.mark_variant == 1 && query.is_delayed_attacking"
						},
						{
							"summon": "query.is_casting && !query.is_delayed_attacking && !query.is_saddled && !query.is_sheared && !query.is_stunned"
						},
						{
							"throw": "query.is_sheared && !query.is_delayed_attacking"
						}
					]
				},
				"attack1": {
					"animations": [
					  "attack1"
					],
					"transitions": [
					 	{
					   		"default": "(query.ground_speed == 0.0 && query.is_on_ground && !query.is_saddled && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned && !query.is_casting) || (query.mark_variant == 2 && query.is_delayed_attacking)"
					 	},
					 	{
					   		"move": "query.ground_speed > 0.0 && !query.is_delayed_attacking && !query.is_sheared && !query.is_saddled && !query.is_stunned && !query.is_casting"
					 	},
						{
							"stay": "query.is_stunned && !query.is_delayed_attacking && !query.is_saddled && !query.is_sheared && !query.is_casting"
						},
						{
							"up": "query.is_saddled && !query.is_delayed_attacking && !query.is_stunned && !query.is_sheared && !query.is_casting"
						},
						{
							"summon": "query.is_casting && !query.is_delayed_attacking && !query.is_saddled && !query.is_sheared && !query.is_stunned"
						},
						{
							"throw": "query.is_sheared && !query.is_delayed_attacking"
						}
					]
				},
				"throw": {
					"animations": [
					  "throw"
					],
					"transitions": [
					 	{
					   		"default": "(query.ground_speed == 0.0 && query.is_on_ground && !query.is_saddled && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned && !query.is_casting) || (query.mark_variant == 2 && query.is_delayed_attacking)"
					 	},
					 	{
					   		"move": "query.ground_speed > 0.0 && !query.is_delayed_attacking && !query.is_sheared && !query.is_saddled && !query.is_stunned && !query.is_casting"
					 	},
						{
							"stay": "query.is_stunned && !query.is_delayed_attacking && !query.is_saddled && !query.is_sheared && !query.is_casting"
						},
						{
							"up": "query.is_saddled && !query.is_delayed_attacking && !query.is_stunned && !query.is_sheared && !query.is_casting"
						},
						{
							"summon": "query.is_casting && !query.is_delayed_attacking && !query.is_saddled && !query.is_sheared && !query.is_stunned"
						},
						{
							"attack1": "query.mark_variant == 1 && query.is_delayed_attacking"
						}
					]
				},
				"summon": {
					"animations": [
					  "attack2"
					],
					"transitions": [
					 	{
					   		"default": "(query.ground_speed == 0.0 && query.is_on_ground && !query.is_saddled && !query.is_delayed_attacking && !query.is_sheared && !query.is_stunned && !query.is_casting) || (query.mark_variant == 2 && query.is_delayed_attacking)"
					 	},
					 	{
					   		"move": "query.ground_speed > 0.0 && !query.is_delayed_attacking && !query.is_sheared && !query.is_saddled && !query.is_stunned && !query.is_casting"
					 	},
						{
							"stay": "query.is_stunned && !query.is_delayed_attacking && !query.is_saddled && !query.is_sheared && !query.is_casting"
						},
						{
							"up": "query.is_saddled && !query.is_delayed_attacking && !query.is_stunned && !query.is_sheared && !query.is_casting"
						},
						{
							"attack1": "query.mark_variant == 1 && query.is_delayed_attacking"
						},
						{
							"throw": "query.is_sheared && !query.is_delayed_attacking"
						}
					]
				}
			}
		}
	}
}